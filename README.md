<<<<<<< README.md
# Wikidata Reference Validator

A simple Flask tool to validate reference URLs used in Wikidata entities.

## Setup

```bash
python -m venv .venv
source .venv/bin/activate   # or .venv\Scripts\activate on Windows
pip install -r requirements.txt
python app.py
```
